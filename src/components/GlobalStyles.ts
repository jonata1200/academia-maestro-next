// src/components/GlobalStyles.ts
// Este componente não é mais necessário - os estilos foram movidos para globals.css
// Mantido apenas para compatibilidade durante a migração

'use client';

export default function GlobalStyles() {
  // Componente vazio - estilos estão em globals.css
  return null;
}